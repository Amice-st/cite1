# Text RPG Website

This repository contains a simple text-based roleplaying game implemented in HTML and JavaScript.

To play, open `index.html` in a web browser. Type commands like `go north` or `take key` to progress through the short adventure.
